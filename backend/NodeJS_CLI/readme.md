To run the CLI, ensure you have npm installed and install
    npm install pg
    npm install commander
    npm install chalk
    npm install ora
    npm install figlet
    npm install inquirer
run npm init 
then run node index.js